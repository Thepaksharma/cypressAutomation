{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "runHeaded": "npx cypress run --headed",
    "RunOnChrome": "npx cypress run --browser chrome",
    "runOnCloud": "npx cypress run --record --key 3f6bafce-0072-4337-9e02-aedacd012e97",
    "runEndToEndTest": "npx cypress run --spec \"cypress/integration/examples/EndtoEndFramework.js\" --headed --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.1",
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "multiple-cucumber-html-reporter": "^3.9.0",
    "neat-csv":"5.1.0"
  },
  "cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true,
      "output": "cypress/cucumberReports/results.json"
    }
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.3",
    "@cypress/browserify-preprocessor": "latest",
    "force": "^0.0.3"
  }
}
